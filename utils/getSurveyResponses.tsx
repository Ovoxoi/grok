const getSurveyResponses = async (surveyId: number) => {
    // Récupérer les réponses du stockage local avec getStoredSurveyResponses
    // Récupérer les réponses du serveur distant
    // Fusionner les réponses et les renvoyer
  };
  